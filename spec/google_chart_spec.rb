require "spec_helper"

RSpec.describe GoogleChart do
  it "has a version number" do
    expect(GoogleChart::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
